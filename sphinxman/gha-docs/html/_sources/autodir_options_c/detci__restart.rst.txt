:term:`RESTART <RESTART (DETCI)>`
"""""""""""""""""""""""""""""""""

      Do restart a DETCI iteration that terminated prematurely? It assumes that the CI and sigma vectors are on disk.

      * **Type**: :ref:`boolean <op_c_boolean>`
      * **Default**: false

